<!-- we will render sepreate nav bars depending on if the user signed in or not, -->
<% if !user_signed_in? %>
  <%= render '/layouts/nav_guest' %>
  <%else %>
  <%= render '/layouts/nav_user' %>
  <% end %>
